# Cinematic reference application — September 9, 2026

## Source and review boundary

User-supplied Paul J Lipsky tutorial, local MP4, 860.8 seconds. The file remained
local. No speech transcript or full audiovisual analysis was available: Gemini's
file upload was disabled behind sign-in. Read-only browser review sampled the
on-screen demonstration at 00:30, 03:00, 05:00, 07:00, 09:00, 11:00 and 13:00.
Narration and unsampled footage are not claimed as reviewed. A web search did not
produce usable primary tutorial notes; no mirror was used as authority.

Observed: at 03:00 the brief establishes audience, inquiry path and honest content;
at 05:00 it revises the visual into a larger opening composition; at 07:00 it
refines a consistent cabin image; at 09:00 the visible prompt specifies a pinned,
scroll-scrubbed scene; at 11:00 a varied image gallery offers larger views.
These are reference observations, not instructions authorizing its providers,
publishing actions, generated claims or changes to this fleet's stack.

## Plan → critique → bounded implementation

1. Give website proof a large media stage and a clear editorial caption/body.
   Keep source descriptions, metrics, URLs and attribution. Add a native modal
   for inspecting the existing stills, and user-controlled video playback.
2. Turn the roadmap spotlight into an explorable project stage driven by the
   existing 21-build data: selection updates the title, source description, state
   and detail jump together. Keep the complete native build list underneath.
3. Retain mobile-first flow, original logo, source truth and shared motion controls.
   Use native elements and short transition feedback; no dependencies or downloads.

Critique: a long pinned cinematic intro can delay the sales action on a phone.
The supplied cabin imagery cannot represent M3MM client proof. Apply the larger
visual composition and inspectable gallery using our own existing media. Do not
copy the cabin, add a video preload to the hero, or invent proof. The roadmap's
selected state must remain useful without motion; its full list must work without
JavaScript. This pass is CompanySite only, with other products parked in TODO.

## Findings → evidence → fix → priority

| Finding | Evidence | Fix | Priority |
| --- | --- | --- | --- |
| Next-launch spotlight cannot explore the rest of the queue | Previous roadmap aside rendered only the next DROP | Source-backed 21-build stage; keep full grouped list below | P1 |
| Small alternating media boxes limit proof inspection | Previous CaseStudy media occupied half the desktop panel | Full-width media with editorial copy and native still viewer | P1 |
| Proof video starts without an explicit play action | Previous data-lazy hydration called play on intersection | Native controls, preload none, requested playback, pause offscreen | P1 |
| Floating review action can intercept gallery clicks | Initial 320px pointer test failed opening the viewer | Suppress shortcut over interactive proof/stage; remove hidden tab stop | P1 |
| Narrow build index clips visible controls | Initial axe run failed three narrow roadmap widths | Native chooser at 1100px and below; desktop keyboard index | P1 |
| Existing Aries poster is an empty title card | Direct poster/media review | Uncropped 00:03 still from existing walkthrough | P2 |
| Existing screenshot details need publication review | Enlarged Big7 reveals placeholder phone/license line; Aries contains ratings/counts | Record Michael's source-media approval/replacement gate | P1 before publication |

## Verification plan

Build/placeholder check, Vitest, 60-route/width axe sweep, canonicals and Astro
check. Exercise stage selection by keyboard, direct jumps, modal Escape/focus
return, no-JS behavior, motion reduction, image loading and video no-autoplay.
Inspect home, Websites and Roadmap at 375/1440 and overflow at 320/768 too.

## Implementation review — September 10

The first browser checks exposed three clipped-index contrast results at narrow
widths; the desktop index now yields to the native chooser at 1100px and below.
A pointer-click check caught the floating review action covering the gallery
button. Interactive proof/stage areas now suppress that shortcut, including its
keyboard stop. The native viewer also gets an explicit solid heading background
so its text has a determinate contrast surface in the top layer.

Visual inspection found the previous Aries poster was a mostly empty title card.
Its replacement is a single uncropped 00:03 frame from the already-shipped
`public/videos/aries-scroll-v2.mp4`, encoded to WebP. This is existing project proof,
not new client photography or generated tutorial imagery. The original walkthrough
and official logo remain unchanged. Portrait video is contained, never cropped.

Final output and verified limitations: `CINEMATIC_VERIFICATION_2026-09-10.md`.
